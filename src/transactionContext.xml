<?xml version="1.0" encoding="utf-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 配置c3p0数据库连接池信息 -->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="com.mysql.cj.jdbc.Driver"></property>
        <property name="jdbcUrl" value="jdbc:mysql:///hbnu?serverTimezone=GMT&amp;useSSL=false&amp;characterEncoding=utf-8"></property>
        <property name="user" value="root"></property>
        <property name="password" value="chendikai"></property>
    </bean>

    <!-- 1、配置事务管理器 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"></property>
    </bean>

    <!-- 2、配置注解驱动 -->
    <tx:annotation-driven transaction-manager="transactionManager"></tx:annotation-driven>

    <!--
    &lt;!&ndash; 2、配置事务增强 &ndash;&gt;
    <tx:advice id="txAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            &lt;!&ndash; 增强方法匹配规则（account*:匹配以account开头的方法）&ndash;&gt;
            <tx:method name="account"/>
        </tx:attributes>
    </tx:advice>

    &lt;!&ndash; 3、配置切面 &ndash;&gt;
    <aop:config>
        &lt;!&ndash; 配置切入点 &ndash;&gt;
        <aop:pointcut id="pointcut1" expression="execution(* com.hbnu.transaction.service.UserService.*(..))"></aop:pointcut>

        &lt;!&ndash; 配置切面 &ndash;&gt;
        <aop:advisor advice-ref="txAdvice" pointcut-ref="pointcut1"></aop:advisor>
    </aop:config>
    -->

    <!-- 配置jdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <!-- 将数据源注入到jdbcTemplate中 -->
        <property name="dataSource" ref="dataSource"></property>
    </bean>

    <bean id="userDao" class="com.hbnu.transaction.dao.UserDao">
        <!-- 将jdbcTemplate注入到UserDao中 -->
        <property name="jdbcTemplate" ref="jdbcTemplate"></property>
    </bean>

    <bean id="userService" class="com.hbnu.transaction.service.UserService">
        <!-- 将userDao注入到UserService中 -->
        <property name="userDao" ref="userDao"></property>
    </bean>
</beans>