<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace是当前文件所在的包名+当前文件名，后面程序中通过namespace + id来执行sql语句 -->
<mapper namespace="UserMapper">
    <!-- select、update、delete、insert这些标签可用于数据库的crud操作
     resultType:结果集类型，List<User>
     resultMap:复杂的结果类型（多表联合查询）
     reultType和resultMap不能同时存在
     -->
    <select id="findAll" resultType="com.hbnu.pojo.User">
        select * from tb_user
    </select>

    <!-- 1、添加用户信息 -->
    <insert id="insert">
        insert into tb_user(username, password, email) values ('张无忌', '123456', 'zhangwuji@163.com')
    </insert>

    <!-- 2、修改用户信息 -->
    <update id="update">
        update tb_user set password = '987654' where username = '张无忌'
    </update>

    <!-- 3、删除用户信息 -->
    <delete id="delete">
        delete from tb_user where username = '张无忌'
    </delete>

    <!-- 4、查询指定用户信息 -->
    <select id="selectOne" resultType="com.hbnu.pojo.User">
        select * from tb_user where username = 'chendikai'
    </select>
</mapper>